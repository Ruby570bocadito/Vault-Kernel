# Ronda 2026-09-15 13:34 — Informe de PULIMIENTO

- **Fecha y hora de la ronda:** 2026-09-15 10:59–13:34 (Europe/Madrid, CEST)
- **Agente:** único — ROL 3 de la ronda 6
- **Contexto previo:** informes del Director e Implementaciones de esta
  ronda (mismo timestamp); el detalle de los fixes de ROL 4 vive en
  `z_bugs/` (mismo timestamp).

## Resumen ejecutivo

El pulido de esta ronda documenta la nueva superficie (`capture`,
`keylog --output`, completions), ejecuta la **reestructuración
condicionada de SCHEMAS.md por comando** — el trigger que el backlog de
v3.8 dejó escrito se dispara con el cuarto documento JSON — y corrige
los dos errores documentales que la auditoría del Director midió contra
el código (conteos de ioctls y de comandos del README). Además: job de
CI nuevo (8 jobs), target `test-completions` en el Makefile (editado
exclusivamente vía script que preserva TABs — ver incidente abajo),
CHANGELOG `[3.9]`, ADR 19 y bits de ejecución de los ficheros nuevos.
Limpieza de archivos: cero huérfanos; dependencias: sin cambios (cero
dependencias externas se mantiene); capturas: sin cambios (el GIF sigue
pendiente de VM — backlog).

## Detalle por cambio

### 1. README

- **Correcciones de exactitud (auditoría del Director):** mermaid
  "ioctl interface — 17 commands" → **16**; árbol
  "ioctl.c — (17 ioctls)" → **16**; árbol "cmd/vault_kernel/ — 18
  comandos" → **21** (conteo verificado contra el código: 16 IOCTLs
  0x01–0x10 en core.h/Go/Python; 21 subcomandos en el dispatcher Go).
- Lista de comandos (sección Español): añadidas las líneas de
  `keylog --follow --output cap.log`, `capture` y
  `capture --out ev.json`, con la nota de autocompletado bash
  (instalación y garantía de test en CI).
- Sección "🔄 Novedades v3.8" → **"Novedades v3.9"**: resume la ronda
  (capture, keylog --output, completions + CI 8 jobs, gramáticas
  estrictas completadas, SCHEMAS por comando, suite 34 Go + 42
  Python + 11 completions).
- Badge de versión y pie: 3.8 → **3.9** (bump coordinado con ROL 4;
  README tocado una sola vez, como en las rondas 1-5).
- Sección Testing: `make test` menciona completions; nuevo bloque
  `make test-completions`; párrafo de CI actualizado a 8 jobs.
- Árbol de referencia: `completions/`, `tests/test_completions.sh`,
  `docs/SCHEMAS.md` como índice + `docs/schemas/` por comando,
  CHANGELOG "v3.0 → v3.9".
- Capturas/GIF: sin cambios (el GIF muestra el CLI base v3.3; sigue
  pendiente de regeneración con VM — backlog).

### 2. `docs/SCHEMAS.md` reestructurado por comando (el trigger del backlog v3.8)

- El backlog de la ronda 5 dejó escrito: "si aparece un cuarto
  documento JSON, mover las tablas de SCHEMAS.md a ficheros
  por-comando". `capture` es el cuarto documento: se ejecuta.
- **Nuevo layout:** `docs/SCHEMAS.md` queda como índice (política de
  versionado del campo `schema`, tabla de los cuatro documentos con
  enlaces, notas de compatibilidad) y el contrato vive en
  `docs/schemas/{stats,list,doctor,capture}.md`.
- **`docs/schemas/capture.md` (nuevo):** ejemplo exacto, tabla
  campo-por-campo, política del 0600, semántica de `captured_at`
  (instante del snapshot leído tras los buffers — cota superior, no
  tiempo por evento) y la nota de orden de claves: el envelope es
  contractual e idéntico en ambos clientes; las claves de `stats`
  van ordenadas en AMBOS (Go ordena las claves de mapa al serializar;
  Python ordena explícitamente para mantener la comparabilidad).
- `stats.md`/`list.md`/`doctor.md` heredan el contenido v3.8 con
  ejemplos actualizados a 3.9 y notas cruzadas hacia `capture`.

### 3. Bits de ejecución (política ADR 18 aplicada a los ficheros nuevos)

- `tests/test_completions.sh`: shebang + `chmod +x` (invocable directo
  tras el clone).
- `completions/vault_kernel.bash`: SIN shebang y SIN bit de ejecución —
  se sourcea, no se ejecuta; la política de ADR 18 es "fichero con
  shebang ⇒ ejecutable", no al revés.

### 4. Makefile (`test-completions` + incidente de TABs)

- Nuevo target `test-completions` (ejecuta
  `tests/test_completions.sh`), `test` encadena
  `test-go test-payloads test-completions`, y `make help` refleja el
  target nuevo y la descripción ampliada de `test`.
- **Incidente y política:** al editar el Makefile con el editor del
  agente, la indentación TAB se convirtió en 8 espacios y `make`
  falló con "missing separator" (segundo incidente TABs en dos
  rondas). Restaurado con `git checkout -- Makefile` y re-aplicado el
  cambio con un script Python que escribe bytes `\t` literales;
  verificado con `make -n test`, `make -n test-completions`, `make
  help` y conteo de líneas-TAB. **Política endurecida y anotada en
  ADR 19/ recomendaciones: el Makefile se edita SOLO vía scripts que
  preserven bytes; el Edit tool queda prohibido para ese fichero.**

### 5. CI — de 7 a 8 jobs

- Job nuevo `Bash completions (functional)`:
  `bash tests/test_completions.sh` en `ubuntu-24.04` — no root, no
  kernel, no paquete bash-completion.
- Job `Shellcheck` ampliado a 7 ficheros: se añaden
  `tests/test_completions.sh` y `completions/vault_kernel.bash`
  (ambos shellcheck-limpios antes del wiring).
- Los ficheros `docs/schemas/*.md` y el resto de la ronda no requieren
  wiring adicional (la job Python ya cubre `client/` y `tests/python/`
  por ruff; go test por glob de paquetes).

### 6. `docs/ADR.md` — ADR 19

- **ADR 19 (v3.9):** decisión completa de `capture` (evidencia,
  timestamp del snapshot, 0600, orden contractual), `keylog --output`
  (transcripción fiel, apéndice, apertura perezosa), política de
  autocompletado (autocontenida, dos clientes, test funcional) y
  cierre de la política de gramáticas estrictas (todo parser puro +
  rechazo de exceso como regla global, no caso a caso).

### 7. CHANGELOG — entrada `[3.9]`

- Añadida con la estructura de las rondas previas (Añadido / Corregido
  con tabla bug→fix de 5 filas / Documentación / Auditoría de la
  ronda) y referencias cruzadas a ADR 19, `docs/schemas/` y los tests
  nuevos. Encabezados `## [3.x]` verificados tras la edición (10
  entradas intactas de 3.0 a 3.9).

### 8. Archivos innecesarios eliminados en esta ronda

- Ninguno: `git status` contiene solo las modificaciones esperadas y
  los ficheros nuevos de la ronda (4 informes + `completions/` +
  `docs/schemas/` ×4 + `tests/test_completions.sh`). Los arneses de
  verificación viven FUERA del repo (regla de rondas previas).
- `git status --ignored` revisado: `.ruff_cache/` excluido (doble vía
  desde v3.8), sin artefactos de build ni payloads generados.

### 9. Dependencias

- **Go:** `go.mod` sigue en cero dependencias externas; los cambios de
  esta ronda usan solo stdlib (`fmt`, `os`, `strconv`, `strings`,
  `time`, `encoding/json`, `testing`).
- **Python:** solo stdlib — `build_capture_bundle` y el sink usan
  `os`/`time`/`json` ya importados; los tests nuevos añaden `tempfile`
  y `os` locales, ambos stdlib. CI sigue fijando `ruff==0.16.7`.
- **Bash:** el script de completions usa solo builtins
  (`complete`/`compgen`/`mapfile`) — NO requiere el paquete
  bash-completion; es la decisión que hace posible el test funcional
  en CI sin instalar nada.
- **Módulo:** sin cambios de headers ni de ABI (v3.9 es cliente y
  documentación; `src/core.h` solo cambia la cadena de versión).

### 10. Interfaz web

N/A (sin interfaz web en el proyecto).

## Bugs o problemas de seguridad detectados (para el ROL 4)

Gestionados en esta misma ronda: cierre sistémico de gramáticas Go,
validación `shell`/`hide-pid` en ambos clientes y fix del one-shot
vacío de `keylog` Python (ver `z_bugs/` para severidad, causa raíz y
verificación). El README ya describe el comportamiento nuevo en
"Novedades v3.9" y la tabla "Corregido" del CHANGELOG cubre los 5
fixes.

## Pendientes de pulido para próximas rondas

1. Regenerar `docs/images/demo.gif` (pendiente desde v3.3) — requiere VM.
2. Tabla de estimaciones de líneas del ADR (sección 5) — cosmético,
   arrastra desde la ronda 2.
3. Numeración del ADR: salta de la sección 7 a la 9 (hueco histórico;
   renumerar tocaría todos los títulos — solo si se toca el ADR por
   otro motivo).
4. Evaluar `-Wall` sin los `-Wno-*` del `src/Makefile` en una ronda con
   VM disponible.
5. Autocompletado zsh (nuevo; solo si el owner usa zsh en el lab).
