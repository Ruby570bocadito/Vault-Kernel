# Ronda 2026-09-15 01:03 — Informe de PULIMIENTO

- **Fecha y hora de la ronda:** 2026-09-15 00:54–01:03 (Europe/Madrid)
- **Agente:** único — ROL 3 de la ronda 3
- **Contexto previo:** informes del Director e Implementaciones de
  esta ronda (mismo timestamp).

## Resumen ejecutivo

El foco de esta ronda fue documentar la nueva superficie JSON y
restaurar la exactitud de la documentación existente: el README llevaba
desde v3.5 una sección "Novedades v3.4" (dos versiones antigua), un
conteo erróneo de jobs de CI y una referencia de CHANGELOG desfasada;
el CHANGELOG tenía un typo de la ronda 2 (`keylog --follow s]`). Añadidos
los comandos nuevos al README, el ADR 16 y la entrada `[3.6]` del
CHANGELOG (por coordinación de versiones, la escribe ROL 4 y se documenta
en su informe). Limpieza de archivos: cero huérfanos; revisión de
dependencias: sin cambios (cero dependencias externas se mantiene).

## Detalle por cambio

### 1. README

- Lista de comandos: añadidos `doctor --json` y `list --json` con
  descripciones (sección Español).
- Sección "🔄 Novedades v3.4" → **"Novedades v3.6"**: el contenido
  resumido de v3.4 llevaba dos versiones sin rotar (el detalle vive en
  el CHANGELOG); ahora resume esta ronda (parser compartido, check de
  versión de `doctor` restaurado, `--interval` en ms, `list --json` /
  `doctor --json`, tests Python en CI).
- Conteo de jobs de CI en el árbol de estructura: "6 jobs" → **7 jobs**
  con nombres reales (Go, kernel runner, kernel matrix docker 5.15/6.8,
  shellcheck, payloads, python+tests) — databa de antes de añadir el
  segundo job de la matriz.
- Referencia del árbol: "CHANGELOG.md … v3.0 → v3.4" → **v3.0 → v3.6**.
- Badge de versión y pie: 3.5 → **3.6** (bump coordinado con ROL 4,
  README tocado una sola vez, igual que en las rondas 1 y 2).
- Capturas/GIF: sin cambios (el GIF muestra el CLI base; sigue
  pendiente de regeneración con VM — backlog).

### 2. `docs/ADR.md` — ADR 16

- **ADR 16 (v3.6):** salida máquina JSON de los CLIs con un único
  parser compartido — contexto (el bug de pares múltiples), decisión
  (parser por tokens como fuente de verdad por cliente; el formato de
  texto del kernel NO cambia; los parsers son código de producción y
  por tanto viven en el repo con tests en CI, a diferencia de los
  arneses) y consecuencias (esquemas estables testados; el contexto de
  sección protege nombres de fichero tipo `pid: 5`).
- Verificado durante la revisión: sospecha de párrafo duplicado en el
  ADR 15 — **falsa alarma** (un único "Consecuencias" en la línea 296;
  comprobado a nivel de fichero, fue un artefacto de render del
  terminal, como el de ci.yml en la ronda 1).

### 3. CHANGELOG — typo de la ronda 2

- Corregido `- **\`keylog --follow s\`]**` →
  `- **\`keylog --follow [ms]\`**` (la entrada `[3.5]` documentaba el
  flag con las unidades mangleadas; verificado byte a byte con `od`).

### 4. Archivos innecesarios eliminados en esta ronda

- Ninguno: `git status` limpio tras el trabajo (solo modificaciones y
  ficheros nuevos esperados). Los arneses de esta ronda
  (`repro_r3_stats_parse.py`, `repro_r3_interval_unit.py`,
  `verify_r3_interval_fix.py`) viven FUERA del repo
  (`scripts/` de la estación de trabajo), según la regla de rondas
  previas.
- `.gitignore`: sin cambios necesarios — ya cubre `__pycache__/`
  (generado ahora por los tests) y los artefactos existentes.

### 5. Dependencias

- **Go:** `go.mod` sigue en cero dependencias externas (el parser vive
  en el paquete interno `vaultkernel` del propio módulo).
- **Python:** solo stdlib — el CLI ya importaba `json`; los tests usan
  `unittest`/`unittest.mock`/`contextlib`/`importlib` (stdlib), sin
  pytest ni dependencias nuevas. CI sigue fijando `ruff==0.16.7`
  (ahora también sobre `tests/python/`).
- **Módulo:** sin cambios de headers ni de ABI (los 3 comandos JSON son
  clientes de los mismos ioctls).

### 6. Interfaz web

N/A (sin interfaz web en el proyecto).

## Bugs o problemas de seguridad detectados (para el ROL 4)

Gestionados en esta misma ronda por ROL 2/ROL 4: parser de `GET_STATS`
(`stats --json` corrupto + check de versión de `doctor` muerto) y
unidades de `--interval`. Ver `z_bugs/` para severidad y verificación.

## Pendientes de pulido para próximas rondas

1. Regenerar `docs/images/demo.gif` (pendiente desde v3.3) — requiere VM.
2. Añadir campo `"schema"` a los JSON de los CLIs si la superficie sigue
   creciendo (propuesta del backlog de Implementaciones).
3. Tabla de estimaciones de líneas del ADR (sección 5) — cosmético,
   arrastra desde la ronda 2.
4. Evaluar `-Wall` sin los `-Wno-*` del `src/Makefile` en una ronda con
   VM disponible.
