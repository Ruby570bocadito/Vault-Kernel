# Ronda 2026-09-15 12:38 — Informe de PULIMIENTO

- **Fecha y hora de la ronda:** 2026-09-15 10:15–12:38 (Europe/Madrid, CEST)
- **Agente:** único — ROL 3 de la ronda 5
- **Contexto previo:** informes del Director, Implementaciones y
  Bugs/Seguridad de esta ronda (mismo timestamp).

## Resumen ejecutivo

El pulido de esta ronda documenta la nueva superficie (`--timestamps`,
`version` en Python, SCHEMAS.md) y ejecuta tres acciones de higiene que
arrastraban varias rondas: **bits de ejecución** para los 7 ficheros
con shebang (solo `test_payloads.sh` lo tenía), **`make help`** alineado
con los targets reales y **`.ruff_cache/`** explícito en `.gitignore`.
Limpieza de archivos: cero huérfanos; dependencias: sin cambios (cero
dependencias externas se mantiene); capturas: sin cambios (el GIF sigue
pendiente de VM — backlog).

## Detalle por cambio

### 1. README

- Lista de comandos (sección Español): añadida la línea
  `vault_kernel keylog --follow --timestamps` junto a las existentes de
  `keylog`, con descripción de la marca `[HH:MM:SS]` por evento.
- Sección "🔄 Novedades v3.7" → **"Novedades v3.8"**: resume esta ronda
  (timestamps, paridad de comandos cerrada — `version` en Python y
  validación de PID en Go, gramática estricta de `keylog`, SCHEMAS.md,
  higiene de repo con la suite a 23 tests Go + 30 unittest Python).
- Badge de versión y pie: 3.7 → **3.8** (bump coordinado con ROL 4;
  README tocado una sola vez, como en las rondas 1-4).
- Árbol de referencia: añadido `docs/SCHEMAS.md` y la referencia del
  CHANGELOG actualizada a "v3.0 → v3.8".
- Capturas/GIF: sin cambios (el GIF muestra el CLI base v3.3; sigue
  pendiente de regeneración con VM — backlog).

### 2. Bits de ejecución (nuevo en esta ronda)

- **Qué:** 7 ficheros con shebang versionados como `100644` —
  `client/vault_kernel_cli.py`, `payloads/payload.py`,
  `payloads/builder.sh`, `tests/integration.sh`,
  `tests/python/test_cli_parsing.py`, `docker/build.sh`,
  `docker/test.sh` — ahora `100755` vía `git update-index --chmod=+x`
  (solo el índice cambia; el contenido es intacto).
- **Por qué:** un fichero ejecutable por shebang debe poder ejecutarse
  directo tras el clone (`./client/vault_kernel_cli.py status`); además
  elimina los avisos `EXE001` de ruff sobre ambos ficheros Python.
- **Impacto:** UX de lab (sin prefijos `python3`/`bash` obligatorios) y
  semántica git correcta. Nada se rompe: los usos documentados
  (`bash tests/...`, `python3 ...`) siguen idénticos.

### 3. `make help` (nuevo en esta ronda)

- **Qué:** la ayuda del Makefile raíz databa de antes de que existieran
  `test-payloads`, `test-go` y `docker-test`, y describía `test` como
  "Run Go unit tests" cuando hoy ejecuta Go + regresión de payloads.
- **Nuevo texto:** `test` (Go + payloads, "no root, no VM"),
  `test-go`, `test-payloads`, `test-integration` (VM con el módulo
  cargado) y `docker-test`.
- **Impacto:** documental. Verificado con `make help` y con `od -c`
  que las recetas conservan sus TABs reales (una edición intermedia
  los convirtió en espacios y `make` falló — detectado por el smoke
  `make -n` de la propia ronda y corregido antes del commit).

### 4. `.gitignore` (nuevo en esta ronda)

- Añadido `.ruff_cache/` con comentario de intención: hasta ahora el
  caché solo quedaba excluido por el `.gitignore` INTERNO que ruff
  crea dentro del propio directorio; si ese fichero desaparece o el
  caché se regenera de otra forma, los ficheros aparecerían como no
  trackeados. Defensa en profundidad, sin cambio de comportamiento
  observable hoy.

### 5. `docs/ADR.md` — ADR 18

- **ADR 18 (v3.8):** paridad de superficie de comandos (give-root Go /
  version Python / gramática de keylog), semántica de `--timestamps`
  (marca del sondeo, no de la pulsación — cota honesta) y política de
  bits de ejecución; incluye el método definitivo de verificación de
  caracteres (`od -c`) como consecuencia de la auditoría de esta
  ronda.

### 6. CHANGELOG — entrada `[3.8]`

- Añadida con la estructura de las rondas previas (Añadido / Corregido
  con tabla bug→fix / Documentación e higiene / Auditoría de la ronda)
  y referencias cruzadas a ADR 18, SCHEMAS.md y los tests nuevos.
  Encabezados `## [3.x]` verificados tras la edición (9 entradas
  intactas de 3.0 a 3.8).

### 7. Archivos innecesarios eliminados en esta ronda

- Ninguno: `git status` contiene solo las modificaciones esperadas y
  los 5 ficheros nuevos de la ronda (4 informes + `docs/SCHEMAS.md`).
  Los arneses de verificación de esta ronda viven FUERA del repo
  (regla de rondas previas).
- `git status --ignored` revisado: `.ruff_cache/` sigue excluido (ahora
  por doble vía), no hay artefactos de build ni payloads generados.

### 8. Dependencias

- **Go:** `go.mod` sigue en cero dependencias externas; los cambios de
  esta ronda usan solo stdlib (`fmt`, `os`, `strconv`, `strings`,
  `time`, `testing`).
- **Python:** solo stdlib — `format_keylog_event` y
  `format_version_line` usan `time` ya importado; los tests nuevos no
  añaden imports beyond stdlib (`mock`, `contextlib`, `io`). CI sigue
  fijando `ruff==0.16.7`.
- **Módulo:** sin cambios de headers ni de ABI (v3.8 es cliente y
  documentación; `src/core.h` solo cambia la cadena de versión).

### 9. Interfaz web

N/A (sin interfaz web en el proyecto).

## Bugs o problemas de seguridad detectados (para el ROL 4)

Gestionados en esta misma ronda: validación del PID de `give-root` en
Go y gramática estricta de `keylog` (ver `z_bugs/` para severidad,
causa raíz y verificación). El README ya describe el comportamiento
nuevo en "Novedades v3.8".

## Pendientes de pulido para próximas rondas

1. Regenerar `docs/images/demo.gif` (pendiente desde v3.3) — requiere VM.
2. Tabla de estimaciones de líneas del ADR (sección 5) — cosmético,
   arrastra desde la ronda 2.
3. Numeración del ADR: salta de la sección 7 a la 9 (hueco histórico,
   sin enlaces rotos; renumerar tocaría todos los títulos — solo si se
   toca el ADR por otro motivo).
4. Evaluar `-Wall` sin los `-Wno-*` del `src/Makefile` en una ronda con
   VM disponible.
