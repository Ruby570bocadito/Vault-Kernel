# Ronda 2026-09-15 17:08 — Informe de PULIMIENTO

- **Fecha y hora de la ronda:** 2026-09-15 16:20–17:08 (Europe/Madrid, CEST)
- **Agente:** único — ROL 3 de la ronda 9
- **Contexto previo:** informes del Director e Implementaciones de
  esta ronda (mismo timestamp); el detalle del contrato de exit
  codes y del bump vive en `z_bugs/` (mismo timestamp).

## Resumen ejecutivo

El pulido de esta ronda mantiene la promesa visual del mandato del
owner (README con imágenes y GIFs REALES) sincronizada con la
versión: **demo.gif se regenera desde la salida REAL del binario
v3.12** — ahora con el bloque `$?` en pantalla que retrata el
contrato de exit codes (0/2/1 visibles) — y el README se actualiza
en ambas secciones (nueva sección de códigos de salida con tabla y
ejemplos, comandos nuevos, conteos reales, novedades de ronda). El
contrato documental se mantiene al día (`capture.md` con los campos
aditivos, CHANGELOG [3.12], ADR 22). Limpieza de archivos: cero
huérfanos (el generador del GIF vive en el espacio de trabajo del
agente, fuera del repo — política de arneses); dependencias: sin
cambios externas (Pillow solo en el espacio del agente, nunca del
repo); Makefile: NO se edita (política ADR 19 — cuarta ronda
consecutiva sin incidentes de TABs); exec bits verificados.

## Detalle por cambio

### 1. `docs/images/demo.gif` — regenerado con salida REAL v3.12

- **Contenido (todo capturado del binario real en este entorno):**
  `version` (+ `echo $?` → **0**), `status` (honesto: "module is NOT
  loaded"), `status --json` (el quinto documento, resaltado en azul),
  `magic-encode pwn 4444` (hash 0xBCBF real), `hide-file a b`
  (warning no-root por stderr + error de gramática + `echo $?` →
  **2**) y `hide-file secreto.txt` (error de dispositivo + `echo $?`
  → **1**). Los `$?` se capturan del PROCESO real, no se escriben a
  mano.
- **Técnica:** render de terminal GitHub-dark con Pillow (880×786),
  efecto de tipeo 3 chars/frame, paleta adaptativa de 64 colores con
  dithering Floyd-Steinberg, ~305 fotogramas, 1.8 MB. Barra de título
  con el rótulo "real v3.12 CLI output"; pie fijo que declara
  "real output of the v3.12 Go CLI · exit codes: 0 ok / 1 runtime /
  2 usage · runs without the kernel module".
- **Honestidad:** cada byte de texto visible es salida o código de
  retorno real del binario v3.12; el pie declara qué alcance tiene
  (comandos pre-dispositivo, sin módulo).
- **Verificación visual:** primer y último fotograma inspeccionados
  — la sesión completa cabe sin desbordes (la altura del canvas se
  calcula de las líneas reales de la sesión), los tres códigos del
  contrato (0/2/1) son visibles y coloreados por clase.
- El resto de activos NO cambia: `banner.png` sigue SIN versión
  (decisión anti-obsolescencia ADR 21 — el badge manda),
  `watch-panel.gif` retrata el renderizador real cuyo formato no
  cambió en v3.12, `architecture.png` retrata la topología de `src/`
  que esta ronda no tocó.

### 2. README — actualización integral (ambas secciones)

- **Nueva sección "🚦 Códigos de salida (contrato v3.12)"** (ES,
  tras el tip de autocompletado): tabla `$?` → clase → ejemplos +
  bloque de shell con los DOS ejemplos reales (gramática → 2,
  dispositivo → 1) y la mención a los tests que lo fijan
  (`TestGrammarErrorsAreUsage`, `TestExitCodeContract`).
- **Lista de comandos (ES):** `watch --count 5` y `help` nuevos;
  la galería retitula el pie del GIF a v3.12 con el contrato de
  `$?`.
- **Testing (ES):** conteos reales al cierre (46 Go + 82 Python +
  15 completions + 13 payloads); comentario del unittest actualizado
  (exit codes, watch --count).
- **Árbol (ES):** `cmd/vault_kernel/` a "23 comandos", CHANGELOG
  "v3.0 → v3.12".
- **Novedades v3.11 → v3.12:** seis bullets al cierre real de la
  ronda (contrato 0/1/2, gramática antes de dispositivo, host
  context de capture, `watch --count`, help + invocación desnuda,
  demo.gif con `$?` + conteos).
- **Inglés:** highlights actualizados (finite watch windows,
  exit-code contract como bullet propio, host context en
  observability), Testing a los conteos reales; badge y pie ya a
  3.12 por el bump coordinado.
- Verificado: sin referencias a una era antigua; el badge (única
  fuente de verdad de la versión) y los textos citan 3.12.

### 3. `docs/schemas/capture.md` — el host context documentado

- Ejemplo JSON con `hostname`/`kernel_release` en su posición
  contractual; dos filas nuevas en la tabla de campos; nota de
  ADITIVIDAD al pie (el envelope se queda en 1; fuentes exactas de
  ambos clientes y fallback a `""`). Ejemplos de versión a 3.12 en
  los cinco contratos (`capture/doctor/stats/status` + SCHEMAS.md
  hereda por referencia).

### 4. CHANGELOG — entrada `[3.12]`

- Estructura de rondas previas: **Añadido** (5 bloques: contrato
  0/1/2, capture con host, `watch --count`, subcomando help, tests
  nuevos con conteo al cierre), **Corregido** tabla de 5 filas
  (matriz de exit codes con la reproducción celda a celda, help
  faltante en Python, ioctl exit-0, normalización `usage: `,
  demo.gif de la era v3.11), **Documentación** (README,
  capture.md, CHANGELOG, ADR 22).

### 5. `docs/ADR.md` — ADR 22

- Seis decisiones registradas: contrato de tres clases idéntico en
  ambos clientes; gramática ANTES de dispositivo (cierre de la
  deuda de la ronda 4); `usageError` como tipo de la capa de
  gramática y el fix del exit-0 de `_ioctl`; invocación desnuda
  como error de uso + subcomando `help`; `watch --count N`
  (exclusión con `--once`, un solo camino de despedida); host
  context aditivo en `capture`. Consecuencias documentadas,
  incluida la deuda conocida restante (intervalo posicional de
  keylog).

### 6. Bits de ejecución

`git ls-files -s` verificado: los ficheros con shebang conservan
100755; los completions se sourcean, no se ejecutan
(política ADR 18); los PNG/GIF son binarios normales (644); sin
modos raros en el árbol.

### 7. Makefile

NO se edita (ningún target nuevo lo requería). Política ADR 19
respetada por cuarta ronda consecutiva; `make -n test`,
`make -n test-completions` verificados como smoke de cierre. Nota
ambiental honesta: el dry-run recursivo de `make kernel` toca
`/lib/modules/.../build`, ausente en este sandbox — el build real
lo hace CI (sin cambio de repo).

### 8. CI

Sin cambios de jobs (8). Ficheros nuevos de la ronda: los 4 informes
(Markdown) y el GIF regenerado — ningún wiring nuevo requerido; la
suite de completions extendida (10 flags, 23 comandos) corre dentro
del job existente.

### 9. Archivos innecesarios eliminados en esta ronda

- El generador del GIF (`scripts/make_demo_gif.py` del agente)
  vive FUERA del repo (espacio de trabajo del agente) — política de
  arneses. `git status` al cierre: solo los cambios previstos, sin
  artefactos de build, sin PNGs intermedios, sin `/tmp` filtrado
  al árbol.

### 10. Dependencias

- **Go:** `go.mod` sigue en cero dependencias externas (`errors`,
  `io`, `runtime` son stdlib).
- **Python:** solo stdlib (`platform` nuevo, ya en stdlib; los
  tests nuevos no añaden imports externos).
- **Pillow:** usado SOLO por el generador del GIF en el espacio del
  agente — jamás es dependencia del repo.
- **Módulo:** `src/core.h` solo cambia la cadena de versión
  (séptima ronda consecutiva sin tocar lógica de kernel).

### 11. Interfaz web

N/A (sin interfaz web en el proyecto).

## Bugs o problemas de seguridad detectados (para el ROL 4)

Gestionados en esta misma ronda (ver `z_bugs/`): la matriz de exit
codes, el exit-0 de `_ioctl`, el subcomando `help` y la
normalización de usage-strings. La galería ya retrata la versión
actual — el activo visual más caro de la ronda 8 se mantiene vivo
con coste de una regeneración.

## Pendientes de pulido para próximas rondas

1. Capturas CON módulo cargado (`watch` en vivo, `stats`,
   hide/unhide end-to-end) para completar la galería — requiere VM
   (arrastrado, el GIF de esta ronda cubre el bloque pre-dispositivo
   + exit codes).
2. Generadores de assets dentro del repo (`tools/` + target `make
   images`) — hoy viven fuera por la política de arneses (INFO).
3. Tabla de estimaciones de líneas del ADR — cosmético, arrastra
   desde la ronda 2.
4. Numeración del ADR: hueco histórico 7→9 (renumerar solo si se
   toca el ADR por otro motivo).
5. Autocompletado fish (INFO — solo si el owner lo usa).
