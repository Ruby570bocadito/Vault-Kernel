# Ronda 2026-09-15 01:38 — Informe de PULIMIENTO

- **Fecha y hora de la ronda:** 2026-09-15 01:10–01:38 (Europe/Madrid)
- **Agente:** único — ROL 3 de la ronda 4
- **Contexto previo:** informes del Director, Implementaciones y
  Bugs/Seguridad de esta ronda (mismo timestamp).

## Resumen ejecutivo

El pulido de esta ronda documenta la nueva superficie (`watch` + campo
`schema`) y corrige dos desajustes de documentación: el conteo de jobs
de CI de la sección inglesa del README ("six jobs", databa de antes de
la matriz Docker) y la rotación de la sección "Novedades" que arrastraba
v3.6. Cierre documental del falso positivo de "corchetes corruptos"
arrastado desde la ronda 1. Limpieza de archivos: cero huérfanos;
dependencias: sin cambios (cero dependencias externas se mantiene);
capturas: sin cambios (el GIF sigue pendiente de VM — backlog).

## Detalle por cambio

### 1. README

- Lista de comandos (sección Español): añadido `vault_kernel watch`
  con descripción ("Vista en vivo de stats + ocultos, refresco 1 s,
  Ctrl-C para salir"), colocado tras `stats --json`.
- Sección "🔄 Novedades v3.6" → **"Novedades v3.7"**: resume esta ronda
  (comando `watch`, esquemas JSON versionados con `schema`, paridad
  total de `doctor --json`, validación de `--interval`, suite a 24
  unittest Python + 20 tests Go).
- Conteo de jobs de la sección **inglesa**: "Honest CI — six jobs" →
  **seven jobs** con nombres reales (Go, kernel runner, matriz Docker
  5.15/6.8, shellcheck, payloads, Python con tests) — el inglés
  arrastraba el conteo de antes del segundo job de la matriz; la
  sección española ya decía 7 desde la ronda 3.
- Badge de versión y pie: 3.6 → **3.7** (bump coordinado con ROL 4,
  README tocado una sola vez, como en las rondas 1-3).
- Referencia del árbol: "CHANGELOG.md … v3.0 → v3.6" → **v3.0 → v3.7**.
- Capturas/GIF: sin cambios (el GIF muestra el CLI base v3.3; sigue
  pendiente de regeneración con VM — backlog).

### 2. `docs/ADR.md` — ADR 17

- **ADR 17 (v3.7):** salida máquina versionada y comando `watch` —
  contexto (sin declaración de versión propia en los JSON; desajuste
  de paridad de `doctor --json` en rutas de fallo), decisión (campo
  `"schema"` entero aditivo en la capa de comando, no en los parsers;
  paridad por punteros en Go; `watch` con render puro testeado y sin
  dependencias; validador dedicado para intervalos) y consecuencias
  (`jq -e '.schema == 1'` como guarda para scripts; los dos clientes
  emiten el mismo documento `doctor --json` en los tres caminos; el
  panel de `watch` es contrato fijado por tests espejo).

### 3. CHANGELOG — entrada `[3.7]`

- Añadida con la estructura de rondas previas (Añadido / Corregido con
  tabla bug→fix / Documentación) y referencias cruzadas a ADR 17 y a
  los tests nuevos. Incluye la nota del cierre del falso positivo de
  corchetes. Encabezados `## [3.x]` verificados tras la edición (8
  entradas intactas de 3.0 a 3.7).

### 4. Archivos innecesarios eliminados en esta ronda

- Ninguno: `git status` tras el trabajo contiene solo las 10
  modificaciones y los 4 ficheros nuevos esperados (2 fuentes Go de
  test + `watch.go` + 4 informes). Los arneses de esta ronda viven
  FUERA del repo (regla de rondas previas).
- `.gitignore`: sin cambios necesarios — `__pycache__/` (regenerado
  por la suite ampliada), `.ruff_cache/`, artefactos de build y
  payloads generados ya estaban cubiertos; verificado con
  `git status --ignored`.

### 5. Dependencias

- **Go:** `go.mod` sigue en cero dependencias externas; `watch.go` y
  el envelope JSON usan solo stdlib (`fmt`, `sort`, `strings`,
  `os/signal`).
- **Python:** solo stdlib — `format_watch_panel`/`watch` usan
  `time`/`json` ya importados; los tests nuevos añaden `argparse`
  (stdlib) y nada más. CI sigue fijando `ruff==0.16.7`.
- **Módulo:** sin cambios de headers ni de ABI (v3.7 es cliente y
  documentación; `src/core.h` solo cambia la cadena de versión).

### 6. Interfaz web

N/A (sin interfaz web en el proyecto).

## Bugs o problemas de seguridad detectados (para el ROL 4)

Gestionados en esta misma ronda: paridad de `doctor --json` en rutas
de fallo y validación de `--interval` (ver `z_bugs/` para severidad,
causa raíz y verificación).

## Pendientes de pulido para próximas rondas

1. Regenerar `docs/images/demo.gif` (pendiente desde v3.3) — requiere VM.
2. Tabla de estimaciones de líneas del ADR (sección 5) — cosmético,
   arrastra desde la ronda 2.
3. Evaluar `-Wall` sin los `-Wno-*` del `src/Makefile` en una ronda con
   VM disponible.
4. Si la superficie JSON crece más, plantear una página de esquemas en
   `docs/` (hoy bastan ADR 17 + campo `schema`).
