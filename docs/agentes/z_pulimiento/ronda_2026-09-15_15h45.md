# Ronda 2026-09-15 15:45 — Informe de PULIMIENTO

- **Fecha y hora de la ronda:** 2026-09-15 15:00–15:45 (Europe/Madrid, CEST)
- **Agente:** único — ROL 3 de la ronda 8
- **Contexto previo:** informes del Director e Implementaciones de
  esta ronda (mismo timestamp); el detalle de los fixes de ROL 4 vive
  en `z_bugs/` (mismo timestamp).

## Resumen ejecutivo

El pulido de esta ronda ejecuta el mandato explícito del owner
(**"actualiza el readme y añade imagenes y gift"**) con la política
de honestidad del proyecto por delante: los cuatro activos visuales
se generan desde SALIDA REAL (binario v3.11 y renderizador real del
panel) o desde la topología real de `src/`, el README se reescribe en
ambas secciones con una galería nueva y los comandos/flags de la
ronda, y el contrato documental se mantiene al día
(`capture.md`, CHANGELOG [3.11], ADR 21). Limpieza de archivos: cero
huérfanos (el arnés de volcado de fotogramas se creó, se usó y se
ELIMINÓ — regla de rondas previas); dependencias: sin cambios
externas; Makefile: NO se edita (política ADR 19 — tercer ronda
consecutiva sin incidentes de TABs); exec bits verificados.

## Detalle por cambio

### 1. Activos visuales — los cuatro, todos reales

- **`docs/images/banner.png` (regenerado, 48 KB, 1500x500):**
  rediseño GitHub-dark con el título bicolor, subtítulo técnico
  (syscall table hooking · pt_regs ABI · x86_64 ≥ 4.17), chips de
  features y el prompt `$ sudo insmod vault_kernel.ko`. SIN número de
  versión incrustado — decisión anti-obsolescencia del Director
  registrada en ADR 21: el badge del README es la única fuente de
  verdad de la versión (el banner anterior anunció "v3.1" durante
  nueve versiones).
- **`docs/images/demo.gif` (regenerado, 1.6 MB, 800x562, 73
  fotogramas únicos):** sustituye al GIF simulado y estático de la
  era v3.1 (137 fotogramas idénticos, salidas inventadas). Cada byte
  del texto visible es la salida REAL del binario Go v3.11 compilado
  en esta ronda: `version`, `status` (honesto: "module is NOT
  loaded"), `status --json`, `magic-encode pwn 4444` (hash 0xBCBF) y
  el error de gramática de `magic-encode pwn` sin puerto. Efecto de
  tipeo 2 chars/fotograma, paleta de 64 colores, pie que declara
  "real output of the v3.11 Go CLI — these commands need no kernel
  module".
- **`docs/images/watch-panel.gif` (NUEVO, 44 KB, 640x456):** tres
  fotogramas producidos por el renderizador REAL
  (`RenderWatchPanelDiff`, volcados por un arnés Go efímero que llamó
  a la función de producción con datos de lab de ejemplo) — muestra
  las anotaciones de cambios de v3.10 (`(was 0)` resaltado en
  amarillo) exactamente como las pinta el código. El pie del GIF
  declara "real renderer output · sample lab data · module required
  in real use" — honestidad por etiquetado, no simulación en
  silencio. El arnés (`zz_dumpframes_test.go`) se creó, ejecutó y
  BORRÓ dentro de la misma ronda (los arneses viven fuera del repo).
- **`docs/images/architecture.png` (NUEVO, 140 KB, 1400x860):**
  diagrama de arquitectura renderizado (renderiza en cualquier
  contexto: móvil de GitHub, editores, embeds) dibujado con la
  topología REAL del proyecto: los tres espacios (usuario → interfaz
  → kernel), los 16 ioctls listados, el canal lateral `kill(pid, 35)`
  con la codificación real, los 7 módulos de hook con sus técnicas y
  el pie de CI (8 jobs). Complementa (no sustituye) el mermaid del
  README, que GitHub renderiza de forma nativa.
- Los generadores de los cuatro assets viven en el espacio de trabajo
  del agente (fuera del repo), coherente con la política de arneses.

### 2. README — reescritura integral (ambas secciones)

- **Galería nueva** (sección 🖼️ al inicio del español): architecture
  a ancho completo + tabla con demo.gif y watch-panel.gif lado a
  lado, cada uno con su pie honesto (qué es salida real y qué es dato
  de ejemplo). La sección 🎬 con el GIF simulado dentro de
  `<details>` DESAPARECE — el material real se muestra directamente.
- **Novedades v3.10 → v3.11:** seis bullets al cierre real de la
  ronda (activos visuales, `capture --stdout`, `keylog --stop-after`,
  POSIX `--`, contrato sin-root, zsh + conteos).
- **Lista de comandos:** `keylog --follow --stop-after 200`,
  `capture --out ev.json --stdout`, nota del escape `--` en
  hide-file.
- **Sección de autocompletado:** bash + zsh con los dos caminos de
  instalación y la mención al test de paridad.
- **Testing:** conteos reales al cierre (43 Go + 73 Python +
  15 completions + 13 payloads).
- **Árbol:** `completions/vault_kernel.zsh`, `watch-panel.gif`,
  `architecture.png`, CHANGELOG "v3.0 → v3.11".
- **Inglés:** highlights actualizados (finite capture windows, two
  completion systems, five JSON documents, watch anotado), quick
  start intacto, architecture.png embebido, badge y pie a 3.11.
- Verificado: sin referencias huérfanas a `docs/images/demo.gif`
  antiguo; los cuatro assets referenciados existen en el árbol.

### 3. `docs/schemas/capture.md` — el modo `--stdout` documentado

- Tercer bullet nuevo en la lista de modos: fichero 0600 Y JSON en
  stdout, resumen a STDERR, pipeline jq limpio, `--stdout` sin
  `--out` aceptado y redundante. Ejemplos de versión a 3.11 en los
  cinco contratos (`capture/doctor/stats/status` + SCHEMAS.md).

### 4. CHANGELOG — entrada `[3.11]`

- Estructura de rondas previas: **Añadido** (5 bloques: capture
  --stdout, stop-after, zsh+paridad, README visual con la Política
  de activos, tests nuevos con conteo al cierre), **Corregido** tabla
  de 5 filas (aviso no-root con el detalle del stdout-roto, POSIX
  `--` con la tabla de paridad, GIF simulado, banner "v3.1", conteos
  de suite), **Documentación** (README, capture.md, CHANGELOG,
  ADR 21). Encabezados verificados: **12 entradas intactas de 3.0 a
  3.11**.

### 5. `docs/ADR.md` — ADR 21

- Seis decisiones registradas: política de activos visuales
  (sin-versión en el arte, solo-salida-real en los GIFs, etiquetado
  de datos de ejemplo, arneses fuera del repo), `capture --stdout`
  (matriz en función pura), `--stop-after` (un solo camino de salida
  limpia), POSIX `--` (tabla reproducida antes de codificar), aviso
  no-root (lista común + stderr), paridad de completados blindada
  por test. Consecuencias documentadas.

### 6. Bits de ejecución

`git ls-files -s` verificado: los ficheros con shebang conservan
100755; `completions/vault_kernel.zsh` NUEVO sin shebang ejecutable
(como su gemelo bash: política ADR 18 — los completados se sourcean,
no se ejecutan); los PNG/GIF son binarios normales (644).

### 7. Makefile

NO se edita (ningún target nuevo lo requería — los tests nuevos
corren dentro de los targets existentes). Política ADR 19 respetada
por tercera ronda consecutiva; `make -n test`,
`make -n test-completions`, `make -n kernel` y `make help` verificados
como smoke de cierre.

### 8. CI

Sin cambios de jobs (8). Ficheros nuevos de la ronda: los assets
binarios no requieren wiring; `completions/vault_kernel.zsh` queda
cubierto por `tests/test_completions.sh` (job completions, que ya
corre el script extendido) y NO entra en shellcheck (no es bash —
la sintaxis zsh se verifica con `zsh -n` cuando el runner lo tiene,
skip determinista si no).

### 9. Archivos innecesarios eliminados en esta ronda

- `client/go/cmd/vault_kernel/zz_dumpframes_test.go` (arnés de
  volcado de fotogramas): creado, ejecutado y eliminado dentro de la
  ronda. `git status` al cierre: solo los cambios previstos, sin
  artefactos de build, sin `/tmp` filtrado al árbol.

### 10. Dependencias

- **Go:** `go.mod` sigue en cero dependencias externas (los cambios
  usan stdlib: `bytes`, `os`, `strconv` ya presentes).
- **Python:** solo stdlib (`subprocess`, `sys`, `argparse` ya
  presentes; los tests nuevos no añaden imports externos).
- **Zsh:** el fichero de completado usa SOLO builtin del sistema de
  completado de zsh — sin paquetes.
- **Módulo:** `src/core.h` solo cambia la cadena de versión (sexta
  ronda consecutiva sin tocar lógica de kernel).

### 11. Interfaz web

N/A (sin interfaz web en el proyecto).

## Bugs o problemas de seguridad detectados (para el ROL 4)

Gestionados en esta misma ronda: aviso no-root y POSIX `--` (ver
`z_bugs/` para severidad, causa raíz y verificación). El hallazgo de
edición del Director (tabs→espacios por old-strings renderizadas)
quedó neutralizado con `gofmt -w` y la lección anotada en su informe.

## Pendientes de pulido para próximas rondas

1. Capturas CON módulo cargado (`watch` en vivo, `stats`, hide/unhide
   end-to-end) para completar la galería — requiere VM (arrastrado,
   ahora con más valor aún).
2. Generadores de assets dentro del repo (`tools/` + target `make
   images`): hoy viven fuera por la política de arneses; si el owner
   quiere regenerarlos sin el agente, es el camino (INFO).
3. Tabla de estimaciones de líneas del ADR (sección 5) — cosmético,
   arrastra desde la ronda 2.
4. Numeración del ADR: hueco histórico 7→9 (renumerar solo si se toca
   el ADR por otro motivo).
5. Autocompletado fish (INFO — solo si el owner lo usa).
