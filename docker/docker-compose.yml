version: "3.9"

services:
  builder:
    build:
      context: ..
      dockerfile: docker/Dockerfile.build
    image: vault_kernel-builder
    container_name: vault_kernel-build
    volumes:
      - ../src:/build/src
      - ./out:/build/out
    command: bash -c "make && cp *.ko /build/out/ && echo '[+] Build complete — vault_kernel.ko in docker/out/'"

  test-victim:
    image: ubuntu:24.04
    container_name: vault_kernel-victim-1
    command: sleep infinity
    networks:
      - vault_kernel-net
    cap_add:
      - NET_ADMIN
    volumes:
      - ./out:/tmp/rootkit

  test-victim-2:
    image: ubuntu:24.04
    container_name: vault_kernel-victim-2
    command: sleep infinity
    networks:
      - vault_kernel-net
    volumes:
      - ./out:/tmp/rootkit

  test-c2:
    image: ubuntu:24.04
    container_name: vault_kernel-c2
    command: sleep infinity
    networks:
      - vault_kernel-net

networks:
  vault_kernel-net:
    driver: bridge
    ipam:
      config:
        - subnet: 172.30.0.0/24
